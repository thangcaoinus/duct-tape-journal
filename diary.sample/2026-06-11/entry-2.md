This is a deliberately old-style entry with no frontmatter block at all — no sentiment, no topic. It's here to prove the parser treats a frontmatter-less file as `meta:{}` and renders it exactly like any other entry.

If it shows up in the reader with no mood chip and no `#topic`, that's the feature working, not a bug.
